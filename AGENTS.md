# AGENTS.md

# Project Overview

このプロジェクトは、ツーリング・ドライブ向けの地図Webサイト「TouringHub」です。

GitHub Pages 上で公開する静的Webサイトとして構成されています。
地図上にツーリング向けスポット、ルート、レイヤー、補助情報を表示し、ユーザーが走行計画を立てやすくすることを目的とします。

## Documentation Encoding

すべてのドキュメントファイルは UTF-8 で扱ってください。

Windows PowerShell でドキュメントを読む場合は、必ず以下のように UTF-8 を明示してください。

```powershell
Get-Content -Raw -Encoding UTF8 <path>
```

以下は禁止です。

```powershell
Get-Content -Raw <path>
```

文字化けして見える場合は、まず UTF-8 明示で再読み込みしてください。
読めないファイルを推測で編集してはいけません。

## Source of Truth

作業前に、可能な限り以下のドキュメントを確認してください。

1. AGENTS.md
2. docs/current-status.md
3. docs/handoff.md
4. docs/architecture.md
5. docs/map-behavior.md
6. docs/data-formats.md
7. docs/verification.md
8. docs/decisions.md

存在しないドキュメントがある場合は、その旨を報告してください。
存在しないことを理由に作業全体を止める必要はありません。

## Project Structure

現在の実際の構成は以下です。

```text
/
├─ AGENTS.md                         # エージェント向け作業ルール、方針、確認事項
├─ README.md                         # ローカルプレビュー方法など、利用者向けの最小説明
├─ index.html                        # 画面構造、外部CSS/JS読み込み、設定txtの描画処理
├─ style.css                         # サイト全体のスタイル、地図、カード、ドロワー、レスポンシブ指定
├─ favicon.ico                       # ブラウザタブなどで使うファビコン
├─ toAI.txt                          # AIへの引き継ぎ用プロンプト、主要ファイルの説明メモ
├─ P35-18_Roadside_Station.geojson   # 道の駅レイヤー用GeoJSONデータ
├─ umap_backup_map.umap              # uMap由来の地図レイヤー/ルート/スポット元データ
├─ conf/
│  ├─ common.js                      # 設定txtのコメント除去など、共通ユーティリティ
│  ├─ map-engine.js                  # Leaflet地図、レイヤー、ピン、座標移動、モバイル操作などの地図ロジック
│  ├─ michi-no-eki.js                # 道の駅GeoJSONを読み込み、クラスタ付きレイヤーとして表示する処理
│  ├─ weather-engine.js              # 天気設定の読み込み、天気API取得、天気UI表示ロジック
│  ├─ weather.txt                    # 天気表示対象のエリア/地点/緯度経度/都道府県設定
│  ├─ nearby.txt                     # 周辺施設検索ボタンの表示名、検索キーワード、アイコン設定
│  ├─ link.txt                       # 「旅の計画」リンク集のカテゴリ、リンク名、URL設定
│  ├─ drawer-links.txt               # ドロワーメニューに表示するリンク設定
│  └─ updates.txt                    # Update Logに表示する更新履歴データ
├─ images/
│  ├─ logo.jpg                       # ヘッダーに表示するサイトロゴ画像
│  └─ ic_logo.png                    # 高速道路ICなど地図上のアイコン画像
└─ old/
   ├─ index.html_20260514            # 2026-05-14時点の旧indexバックアップ
   ├─ index_20260515.html            # 2026-05-15時点の旧indexバックアップ
   └─ index_20260515-2.html          # 2026-05-15時点の追加旧indexバックアップ
```

`docs/`、`css/`、`js/`、`data/` ディレクトリは現時点では存在しません。
構成を確認する場合は、この節よりも必ずリポジトリ上の現物を優先してください。

## Development Policy

### 基本方針

* 変更は最小限にしてください。
* 依頼された範囲外の修正をしないでください。
* 大規模なリファクタリングは、明示的に依頼された場合のみ行ってください。
* ファイル名、フォルダ構成、関数名、データ形式を勝手に変更しないでください。
* 不明点がある場合は、実装前に質問してください。

### 実装前承認

実装を開始する前に、必ず以下を提示してください。

1. 変更対象ファイル
2. 実装方針
3. 既存仕様への影響
4. UIへの影響
5. 確認方法

ユーザーから明示的な承認を得てから実装してください。

ただし、ユーザーが「そのまま実装して」「修正して」など明確に実装を依頼している場合は、過度な確認で止めず、必要最小限の計画提示後に進めてください。

## UI Text Preservation

ユーザーに見える文言は、プロダクト設計の一部です。

以下の文言は、明示的な依頼なしに変更しないでください。

* ボタン名
* 見出し
* 説明文
* エラーメッセージ
* プレースホルダー
* 地図上のラベル
* ポップアップ文言
* 操作案内
* レイヤー名
* スポット名

文言変更が有益だと思う場合は、実装せず提案に留めてください。

## UI Layout Preservation

既存UIの配置、表示順、折りたたみ方式、ボタン位置、地図上の表示位置は、明示依頼なしに変更しないでください。

新機能を追加する場合も、既存UIへの影響を最小限にしてください。

## Map Behavior Policy

地図まわりの変更では、以下に注意してください。

* 初期表示位置・ズームを不用意に変更しないでください。
* 既存レイヤーの表示/非表示仕様を壊さないでください。
* 既存スポットデータの形式を勝手に変更しないでください。
* 地図ライブラリや外部サービスの差し替えは、明示依頼がある場合のみ行ってください。
* モバイル表示で操作不能にならないようにしてください。

## Data Policy

地図データ、スポットデータ、設定データを変更する場合は、以下を確認してください。

* 既存データ形式との互換性
* 既存JSからの参照箇所
* 表示順やカテゴリ分けへの影響
* GitHub Pages上で読み込める相対パスになっているか

データ形式を変更する場合は、関連ドキュメントも更新してください。

### Master Map Data Protection

以下の2ファイルは、マスタ更新時にファイル全体が置き換えられる原本データです。

* `umap_backup_map.umap`
* `P35-18_Roadside_Station.geojson`

通常の機能追加、性能改善、不具合修正、表示調整のために、これらのファイルを直接編集してはいけません。

禁止事項:

* Feature、座標、プロパティ、レイヤーの追加・修正・削除
* 並び替え、整形、圧縮、コメント追加などの書式変更
* アプリ都合によるデータ形式変更
* 元ファイルを手作業で分割し、分割後データを正として運用する変更

許可される更新は、正規のマスタ更新作業として新しいファイル全体へ置き換える場合のみです。
アプリ側の改修が必要な場合は、原本データを加工せず、読み込み処理側で対応してください。

## External Services Policy

外部API、外部サービス、外部ライブラリを追加する場合は、事前に以下を説明してください。

* 追加理由
* 無料枠・課金可能性
* APIキーが必要か
* GitHub Pagesで安全に使えるか
* 代替案

特に、Google Maps API など課金やAPIキー管理が関係するものは、明示承認なしに導入しないでください。

## GitHub Pages Policy

このプロジェクトは GitHub Pages で公開される前提です。

以下に注意してください。

* 静的ホスティングで動作する実装にしてください。
* サーバーサイド処理を前提にしないでください。
* ローカルでは動いても GitHub Pages で動かないパス指定を避けてください。
* `fetch()` を使う場合は、HTTPサーバー経由で確認してください。

ローカル確認例:

```powershell
python -m http.server 8000
```

## Constants and Magic Numbers

調整可能な値は、可能な限り名前付き定数として定義してください。

例:

* ズームレベル
* 表示件数
* 入力文字数制限
* タイムアウト
* タイルサイズ
* レイヤー透明度
* ピン表示時間
* UIのしきい値

処理中に意味不明な数値を直接書かないでください。

## Documentation Maintenance

機能追加・仕様変更・設計変更を行った場合は、関連ドキュメント更新の要否を確認してください。

主なドキュメント役割:

### docs/current-status.md

現在の実装状態を簡潔にまとめるドキュメントです。
実装済み機能、既知の課題、次にやるべきことを記録します。

### docs/architecture.md

システム構成、ファイル構成、地図表示、データ読み込み、主要処理フローをまとめるドキュメントです。

### docs/project-memory.md

過去の判断、試したこと、却下した案、今後忘れたくない背景を残すドキュメントです。

### docs/decisions.md

設計判断を記録するドキュメントです。
特に外部API採用、データ形式変更、UI方針変更などを記録します。

### docs/roadmap.md

今後の開発予定を整理するドキュメントです。

## Verification

変更後は、可能な範囲で以下を確認してください。

* ページが表示されること
* ブラウザコンソールに重大なエラーがないこと
* 地図が表示されること
* 既存レイヤーが壊れていないこと
* 追加・変更した機能が動作すること
* スマホ幅でも大きく崩れないこと

Node.js や追加ツールが使えない場合は、その旨を報告し、可能な代替確認を行ってください。

## Completion Report

作業完了時は、以下を報告してください。

1. 変更したファイル
2. 変更内容の要約
3. 確認した内容
4. 確認できなかった内容
5. ドキュメント更新の有無
6. 追加で気づいた注意点

## Prohibited Actions

明示的な依頼なしに、以下を行わないでください。

* UI文言の変更
* UIレイアウトの大幅変更
* 外部APIの追加
* 課金可能性のあるサービス導入
* 既存データ形式の破壊的変更
* `umap_backup_map.umap` または `P35-18_Roadside_Station.geojson` の直接編集
* 大規模リファクタリング
* フォルダ構成の変更
* 不要な依存関係の追加
* GitHub Pagesで動作しない構成への変更

## Development Experience Assumptions

プロジェクトオーナーはシステム開発経験がありますが、一般的なWeb開発・JavaScript・地図ライブラリ・GitHub Pages固有の知識は前提にしすぎないでください。

重要な技術判断を提案する場合は、理由、メリット、リスクを簡潔に説明してください。
