# Rin's Touring Hub

## Local Preview

This site uses `fetch()` to load local files, so preview it through a local HTTP server instead of opening `index.html` directly.

Run this command from the repository root:

```powershell
python -m http.server 8000
```

Then open:

```text
http://localhost:8000/
```
