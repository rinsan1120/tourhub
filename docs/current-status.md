# TouringHub Current Status

更新日: 2026-06-10

現在の実装状態、既知の課題、次に確認すべきことをまとめます。

## 1. 現在の状態

TouringHub は、静的HTML/CSS/JavaScriptで構成されたツーリング・ドライブ向け地図サイトです。

主な構成:

- エントリーポイント: `index.html`
- スタイル: `style.css`
- 地図ロジック: `conf/map-engine.js`
- 道の駅ロジック: `conf/michi-no-eki.js`
- 天気ロジック: `conf/weather-engine.js`
- 設定データ: `conf/*.txt`
- 地図データ: `umap_backup_map.umap`、`P35-18_Roadside_Station.geojson`

## 2. 実装済み機能

### 地図

- Leaflet + OpenStreetMap による地図表示
- uMap由来データの表示
- 名道ルート表示
- グルメ、温泉、観光、キャンプ場、宿、景勝地表示
- 高速道路IC表示
- 道の駅表示
- Pointレイヤーのクラスタリング
- レイヤー凡例と表示切り替え
- 現在地表示
- 現在地へ移動
- 仮ピン設置
- ポップアップからGoogle Mapsを開く
- 高速/下道ルート検索
- 緯度経度コピー
- 座標移動
- 全画面表示
- Zoomバッジ
- PC右クリックで仮ピン
- モバイル長押しで仮ピン
- モバイルダブルタップズーム
- モバイル2回目タップ保持からの片指ズーム

### 天気

- エリア別天気タブ
- 3日分の天気表示
- Open-Meteo API連携
- 降水確率、最低/最高気温表示
- 現在タブの再取得
- 地点名によるGoogle天気検索
- 天気精度注意メッセージ

### リンク/補助機能

- 周辺施設検索
- 旅の計画リンク集
- ドロワーメニュー
- Update Log
- Googleフォームへのスポット追加協力リンク
- フッターの出典/作者リンク
- Cloudflare Web Analytics

## 3. データ件数

### uMapデータ

| レイヤー | Feature数 | 備考 |
| --- | ---: | --- |
| 名道 | 213 | LineString |
| グルメ | 93 | Point |
| 温泉 | 28 | Point |
| 観光 | 116 | Point 115件、LineString 1件 |
| キャンプ場 | 24 | Point |
| 宿 | 8 | Point |
| 景勝地 | 120 | Point |
| 高速道路IC | 2123 | Point |

### その他

- 道の駅: 1145件
- 天気地点: 9エリア、85地点
- 周辺施設検索: 6件
- 旅の計画リンク: 4カテゴリ、19件
- ドロワーリンク: 2カテゴリ、5件
- Update Log: 20件

## 4. 既知の課題・不明瞭な箇所

- `index.html` に設定txt読み込み用のインラインJSが残っています。
- `style.css` とHTML/JS内インラインスタイルの責務が混在しています。
- `map-engine.js` 内に「聖域：ロジック改変厳禁」というコメントがあり、地図周辺の変更は慎重に扱う必要があります。
- `michi-no-eki.js` は自動実行され、`initMap()` からは明示的に呼ばれません。
- `道の駅` は `map-engine.js` のレイヤー設定にも存在し、将来uMap側に同名レイヤーが入ると重複に注意が必要です。
- 天気APIの精度はUI上で注意喚起されています。
- OpenStreetMapタイル利用ポリシー、Cloudflare Analyticsトークン、高速道路ICデータ更新手順は要確認です。

## 5. 確認済み

- ドキュメントはUTF-8指定で読み込む必要があります。
- ローカル表示確認はHTTPサーバー経由が必要です。
- `docs/architecture.md` を元にドキュメント分割を行い、主要Docsを作成済みです。
- `AGENTS.md` の Source of Truth は現在の `docs/` 構成に同期済みです。

## 6. 次にやるべきこと

- 各ドキュメントを実装変更時の参照先として更新し続ける。
- 地図操作、データ形式、検証手順を実作業で使える粒度に育てる。
- uMapデータ、高速道路ICデータ、外部サービス利用方針などの要確認事項を整理する。
